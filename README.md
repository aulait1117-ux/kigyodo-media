# kigyodo-media

Threads投稿に添付する画像の公開置き場。

Threads API は画像のバイナリを受け取らない。Metaのサーバーが `image_url` を
**自分で取りに来る**ため、画像は認証なしで外から取得できる場所に無ければならない。
それがこのリポジトリ（GitHub Pages で配信）。

- 置くもの：投稿に添付する画像だけ
- 置かないもの：素材の生テキスト・個人情報・鍵の類（このリポジトリは**公開**）
- 追加は `C:\企業道\scripts\threads_attach_image.py` から行う（手で置かない）
